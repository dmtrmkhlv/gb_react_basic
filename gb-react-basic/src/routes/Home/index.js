import React from "react";
import styles from '../../styles/App.module.css';

export const Home = (props) => {
  return (
    <div className={styles.Wrapper}>
      <h1 className={styles.App}>Home</h1>
    </div>
  );
};